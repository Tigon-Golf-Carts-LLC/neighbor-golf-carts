import StateLocationPageTemplate from "./StateLocationPageTemplate";
import { ALL_50_STATES } from "@/data/all50States";

// Alabama
export const AlabamaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'alabama')!;
  return <StateLocationPageTemplate state={state} />;
};

// Alaska
export const AlaskaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'alaska')!;
  return <StateLocationPageTemplate state={state} />;
};

// Arizona
export const ArizonaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'arizona')!;
  return <StateLocationPageTemplate state={state} />;
};

// Arkansas
export const ArkansasPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'arkansas')!;
  return <StateLocationPageTemplate state={state} />;
};

// California
export const CaliforniaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'california')!;
  return <StateLocationPageTemplate state={state} />;
};

// Colorado
export const ColoradoPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'colorado')!;
  return <StateLocationPageTemplate state={state} />;
};

// Connecticut
export const ConnecticutPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'connecticut')!;
  return <StateLocationPageTemplate state={state} />;
};

// Delaware
export const DelawarePage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'delaware')!;
  return <StateLocationPageTemplate state={state} />;
};

// Florida
export const FloridaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'florida')!;
  return <StateLocationPageTemplate state={state} />;
};

// Georgia
export const GeorgiaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'georgia')!;
  return <StateLocationPageTemplate state={state} />;
};

// Hawaii
export const HawaiiPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'hawaii')!;
  return <StateLocationPageTemplate state={state} />;
};

// Idaho
export const IdahoPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'idaho')!;
  return <StateLocationPageTemplate state={state} />;
};

// Illinois
export const IllinoisPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'illinois')!;
  return <StateLocationPageTemplate state={state} />;
};

// Indiana
export const IndianaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'indiana')!;
  return <StateLocationPageTemplate state={state} />;
};

// Iowa
export const IowaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'iowa')!;
  return <StateLocationPageTemplate state={state} />;
};

// Kansas
export const KansasPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'kansas')!;
  return <StateLocationPageTemplate state={state} />;
};

// Kentucky
export const KentuckyPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'kentucky')!;
  return <StateLocationPageTemplate state={state} />;
};

// Louisiana
export const LouisianaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'louisiana')!;
  return <StateLocationPageTemplate state={state} />;
};

// Maine
export const MainePage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'maine')!;
  return <StateLocationPageTemplate state={state} />;
};

// Maryland
export const MarylandPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'maryland')!;
  return <StateLocationPageTemplate state={state} />;
};

// Massachusetts
export const MassachusettsPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'massachusetts')!;
  return <StateLocationPageTemplate state={state} />;
};

// Michigan
export const MichiganPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'michigan')!;
  return <StateLocationPageTemplate state={state} />;
};

// Minnesota
export const MinnesotaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'minnesota')!;
  return <StateLocationPageTemplate state={state} />;
};

// Mississippi
export const MississippiPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'mississippi')!;
  return <StateLocationPageTemplate state={state} />;
};

// Missouri
export const MissouriPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'missouri')!;
  return <StateLocationPageTemplate state={state} />;
};

// Montana
export const MontanaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'montana')!;
  return <StateLocationPageTemplate state={state} />;
};

// Nebraska
export const NebraskaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'nebraska')!;
  return <StateLocationPageTemplate state={state} />;
};

// Nevada
export const NevadaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'nevada')!;
  return <StateLocationPageTemplate state={state} />;
};

// New Hampshire
export const NewHampshirePage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'new-hampshire')!;
  return <StateLocationPageTemplate state={state} />;
};

// New Jersey
export const NewJerseyPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'new-jersey')!;
  return <StateLocationPageTemplate state={state} />;
};

// New Mexico
export const NewMexicoPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'new-mexico')!;
  return <StateLocationPageTemplate state={state} />;
};

// New York
export const NewYorkPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'new-york')!;
  return <StateLocationPageTemplate state={state} />;
};

// North Carolina
export const NorthCarolinaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'north-carolina')!;
  return <StateLocationPageTemplate state={state} />;
};

// North Dakota
export const NorthDakotaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'north-dakota')!;
  return <StateLocationPageTemplate state={state} />;
};

// Ohio
export const OhioPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'ohio')!;
  return <StateLocationPageTemplate state={state} />;
};

// Oklahoma
export const OklahomaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'oklahoma')!;
  return <StateLocationPageTemplate state={state} />;
};

// Oregon
export const OregonPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'oregon')!;
  return <StateLocationPageTemplate state={state} />;
};

// Pennsylvania
export const PennsylvaniaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'pennsylvania')!;
  return <StateLocationPageTemplate state={state} />;
};

// Rhode Island
export const RhodeIslandPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'rhode-island')!;
  return <StateLocationPageTemplate state={state} />;
};

// South Carolina
export const SouthCarolinaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'south-carolina')!;
  return <StateLocationPageTemplate state={state} />;
};

// South Dakota
export const SouthDakotaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'south-dakota')!;
  return <StateLocationPageTemplate state={state} />;
};

// Tennessee
export const TennesseePage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'tennessee')!;
  return <StateLocationPageTemplate state={state} />;
};

// Texas
export const TexasPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'texas')!;
  return <StateLocationPageTemplate state={state} />;
};

// Utah
export const UtahPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'utah')!;
  return <StateLocationPageTemplate state={state} />;
};

// Vermont
export const VermontPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'vermont')!;
  return <StateLocationPageTemplate state={state} />;
};

// Virginia
export const VirginiaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'virginia')!;
  return <StateLocationPageTemplate state={state} />;
};

// Washington
export const WashingtonPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'washington')!;
  return <StateLocationPageTemplate state={state} />;
};

// West Virginia
export const WestVirginiaPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'west-virginia')!;
  return <StateLocationPageTemplate state={state} />;
};

// Wisconsin
export const WisconsinPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'wisconsin')!;
  return <StateLocationPageTemplate state={state} />;
};

// Wyoming
export const WyomingPage = () => {
  const state = ALL_50_STATES.find(s => s.slug === 'wyoming')!;
  return <StateLocationPageTemplate state={state} />;
};
